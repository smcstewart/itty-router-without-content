{
  "name": "itty-router-without-content",
  "version": "1.0.0",
  "description": "Repro of issue with `withContent`.",
  "scripts": {
    "dev": "wrangler dev ./worker.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Steven Stewart",
  "devDependencies": {
    "wrangler": "3.23.0"
  },
  "dependencies": {
    "itty-router": "4.0.27"
  }
}
